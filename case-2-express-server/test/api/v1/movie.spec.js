const { expect } = require("chai");

describe("Check movie routes", () => {
  it("Search movie gives result without errror", () => {
    expect(true).to.be.true;
  });
  it("Detail movie gives result without errror", () => {
    expect(true).to.be.true;
  });
});
